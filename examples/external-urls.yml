---
execution:
  - executor: encarno
    scenario: simple
    concurrency: 1
    hold-for: 1m

scenarios:
  simple:
    default-address: http://localhost:8070/

    requests: urls.txt  # <-- External file is referenced here